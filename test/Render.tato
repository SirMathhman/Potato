val Render = {
  val Component = {
    val render = def => string => ???;
  }

  val DOM = def[element ElementNode, root Component] {
    val value = root.render();
    element.innerHTML = value;
  }
}
