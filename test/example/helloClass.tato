Point[x float, y float] = {
  val ^[] = Point(0, 0)
  $ = {
    $x
    $y
  }

  distance[other *Point] => float = sqrt(
    pow(&other.x, 2) +
    pow(&other.y, 2)
  )
}

val point = Point(3, 4)
val distance = point.distance(Point)
print(distance)
