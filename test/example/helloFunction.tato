empty = {
}
empty()

with Internal.write

singleParameter[value string] = write(value)
singleParameter("A function with one parameter!")

doubleParameter[value0 string, value1 string] = write("Hello " + value0 + value1 + "!")
doubleParameter("Gavin", "Bromberg")

returnFunction => string = "stuff"
write(returnFunction())
